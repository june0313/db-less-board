{{#partial "body"}}
    <div class="container">
        <div class="text-right">
            <a href="/posts/write" class="btn btn-primary" role="button">글쓰기</a>
        </div>

        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Title</th>
                <th scope="col">Contents</th>
                <th scope="col">Writer</th>
            </tr>
            </thead>
            <tbody>
            {{#each posts}}
                <tr>
                    <th scope="row">{{id}}</th>
                    <td>{{title}}</td>
                    <td>{{contents}}</td>
                    <td>{{writer}}</td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>
{{/partial}}

{{> layout/layout}}